$(function(){
  $('#slider-forecast, #slider-live, #slider-report').slick({
    centerMode: true,
    centerPadding: '0px',
    slidesToShow: 1,
    cssEase: 'ease-in-out',
    autoplay: true,
    autoplaySpeed: 5000,
    speed: 500,
  });
});